---
description:
globs:
alwaysApply: false
---
# Key Technologies

This project utilizes the following core technologies:

- **Build Tool/Development Server:** Vite (`[vite.config.ts](mdc:vite.config.ts)`)
- **Language:** TypeScript (`[tsconfig.json](mdc:tsconfig.json)`)
- **UI Framework/Library:** React (inferred, commonly used with Vite/TSX)
- **Styling:** Tailwind CSS (`[tailwind.config.ts](mdc:tailwind.config.ts)`)
- **Backend/Database:** Supabase (integration likely in `[src/services/](mdc:src/services/)` or `[src/lib/](mdc:src/lib/)` using Supabase client libraries)
- **Package Management:** npm (based on `package-lock.json`) or Bun (based on `bun.lockb`) - check `[package.json](mdc:package.json)` scripts for usage.
- **UI Components:** Potentially shadcn/ui or similar (based on `[components.json](mdc:components.json)`).
